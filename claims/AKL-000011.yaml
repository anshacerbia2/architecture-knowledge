id: AKL-000011
record_kind: claim
subject: AKC-000011
predicate: defines
object:
  literal: the intended server effect of repeated identical requests for an HTTP method
statement: For an HTTP method, idempotency means that the intended server effect of multiple identical requests is the same as the effect of one such request.
claim_type: normalized-source-claim
semantic_scope: concept-global
conditions: []
confidence: high
sources:
  - AKS-000011
derived_from_claims: []
status: sourced
notes: Strictly scoped to RFC 9110 HTTP method semantics. Operation deduplication, message replay, and implementation tactics are separate contextual roles on the umbrella concept.
version: 2
